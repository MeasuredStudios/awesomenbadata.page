import * as React from 'react';
import 'twin.macro';
import Layout from '../components/Layout';
import { PageProps } from 'gatsby';

const Home: React.FC<PageProps> = () => <Layout></Layout>;

export default Home;
